<% if user_signed_in? %>
  <%= form_for [post, comment] do |f| %>
    <%= f.text_field :content %>
    <%= f.submit 'コメントする' %>
  <% end %>
<% else %>
  <p>ログインをしてください</p>
  <%= button_to "ログイン", new_user_session_path, class: "btn btn-success" %>
<% end %>